# PowerFit Gym

React + Vite gym website scaffold based on the project documentation.

## Run locally

```bash
npm install
npm run dev
```

## Structure

- `src/pages` for main pages
- `src/sections` for page sections
- `src/components` for reusable UI
- `src/services` for chatbot, API, and Firebase integration
